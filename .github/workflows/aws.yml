name: Deploy to Amazon ECS

on:
  push:
    branches: [ "master" ]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-account-id: ${{ secrets.AWS_ACCOUNT_ID }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, tag, and push image to Amazon ECR
      id: build-image
      run: |
        echo "${{ secrets.DOCKERPASS }}" | docker login -u "lakshya0102" --password-stdin docker.io
        docker pull lakshya0102/lakshya0102:latest
       
        docker tag lakshya0102/lakshya0102:latest 974385115063.dkr.ecr.us-west-2.amazonaws.com/lakshya0102/lakshya0102:latest
        docker push 974385115063.dkr.ecr.us-west-2.amazonaws.com/lakshya0102/lakshya0102:latest
    

        
