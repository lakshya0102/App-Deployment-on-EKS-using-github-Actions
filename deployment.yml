apiVersion: apps/v1
kind: Deployment
metadata:
  name: example.com 
  labels:
    app: Lakshya_app
spec:
  selector:
    matchLabels:
        app: Lakshya_app
  replicas: 3
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: Lakshya_app
    spec:
      containers:
        - name: Lakshya_app
          image: lakshya0102/lakshya0102:latest
          ports:
            - containerPort: 80
              protocol: TCP
          imagePullPolicy: Always
